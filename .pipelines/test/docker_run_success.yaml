version: v0

# Pipeline info
pipeline:
  name: test_pipeline

# List of stages - show order of execution
stages:
  - build
# Stages defined below
jobs:
  # build
  - name: compile
    stage: build
    image: maven:3.8.6
    script:
      - ps -u
      - mvn -v
